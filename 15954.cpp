#include <iostream>
#include <cmath>
#include <algorithm>
using namespace std;
const int MAX=501;
int n,k;
int a[MAX]={0};
int main(int argc, char** argv) {
	cin>>n>>k;
	for(int i=0;i<n;i++)
		cin>>a[i];
	
	return 0;
}
